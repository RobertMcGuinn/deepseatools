## pushing to vlab gitlab (to branch: deepseatools)
git push -u vlab_gitlab master:deepseatools

## pushing to gitlab (to branch: master)
git push gitlab

## pushing to github
git push origin master

## or copy them all at once to push to all three (paste in bash terminal after commits
git push origin master
git push gitlab
git push -u vlab_gitlab master:deepseatools

